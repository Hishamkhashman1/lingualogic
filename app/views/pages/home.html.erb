<%# <!DOCTYPE html>
<html>
<head>
    <script src="https://cdn.jsdelivr.net/npm/phaser@3.60.0/dist/phaser-arcade-physics.min.js"></script>
</head>
<body>
  <h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<div id="game">
<button id="speed+"> + Speed </button>
<button id="speed-"> - Speed </button>

    <script>
    class Example extends Phaser.Scene
    {
        preload ()
        {
            this.load.setBaseURL('https://labs.phaser.io');

            this.load.image('sky', 'assets/skies/space3.png');
            this.load.image('logo', 'assets/sprites/phaser3-logo.png');
            this.load.image('red', 'assets/particles/red.png');
        }

        create ()
        {
            this.add.image(400, 300, 'sky');

            var partspeed = 10

            var particles = this.add.particles(0, 0, 'red', {
                speed: partspeed,
                scale: { start: 2, end: 0.5 },
                blendMode: 'ADD'
            });

            var logospeed = 100
            document.getElementById('speed-display').innerText = `Speed: ${logospeed}`

            var logo = this.physics.add.image(400, 100, 'logo');

            logo.setVelocity(logospeed, logospeed);
            logo.setBounce(1, 1);
            logo.setCollideWorldBounds(true);

            particles.startFollow(logo);

            var eventEmitter = new Phaser.Events.EventEmitter();
            eventEmitter.on('speed-update', () => console.log("speed changed " + logospeed, this));

            function updatespeed(event) {
              var speeddisplay = `Speed: ${logospeed}`;
              document.getElementById('speed-display').innerText = speeddisplay;
              if (logospeed > 500) {
                document.getElementById('speed-display').style.backgroundColor = "green";
              }
              else if (logospeed < 0) {
                document.getElementById('speed-display').style.backgroundColor = "red";
              }
              if (logospeed == 800) {
                document.getElementById('message').innerText = "Congratulations, task solved!";
              }
            }

            eventEmitter.on('speed-update+', () => logospeed += 100, this);
            eventEmitter.on('speed-update+', () => logo.setVelocity(logospeed,logospeed), this);
            eventEmitter.on('speed-update-', () => logospeed -= 100, this);
            eventEmitter.on('speed-update-', () => logo.setVelocity(logospeed,logospeed), this);
            // eventEmitter.on('example-event', () => document.getElementById('speed-display').innerText = `Speed: ${logospeed}`, this);
            document.getElementById('speed+').addEventListener('click', () => eventEmitter.emit('speed-update+'));
            document.getElementById('speed+').addEventListener('click', () => updatespeed());
            document.getElementById('speed-').addEventListener('click', () => eventEmitter.emit('speed-update-'));
            document.getElementById('speed-').addEventListener('click', () => updatespeed());
        }


    }

    const config = {
        type: Phaser.AUTO,
        width: 600,
        height: 400,
        scene: Example,
        physics: {
            default: 'arcade',
            arcade: {
                gravity: { y: 200 }
            }
        }
    };

    const game = new Phaser.Game(config);
    </script>
  </div>

<h2 id="message"></h2>
<p id='speed-display'> Speed: </p>
</body>
</html> %>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lingualogic - Landing Page</title>
    <!-- Link to your CSS file for styling -->
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <!-- Header/Navigation Section -->
    <header>
        <nav>
            <h1>Lingua Logic</h1>
            <ul>
                <li><a href="#about">About</a></li>
                <li><a href="#features">Features</a></li>
                <%# <li><a href="#download">Download</a></li> %>
            </ul>
        </nav>
    </header>

    <!-- Hero Section (Main attention area) -->
    <section id="hero">
        <div class="hero-content">
            <h2>Welcome to the Fun way of coding</h2>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod</p>
            <!-- Call to Action Button -->
            <%= link_to "Play Now!", new_monster_path, class: "cta-button" %>
        </div>
        <!-- Placeholder for a hero image or video -->
        <div class="hero-media">
            <!-- Add an <img> tag or <video> tag here -->
            <img src="placeholder-image.jpg" alt="Game Screenshot/Trailer Placeholder">
        </div>
    </section>

    <!-- About/Description Section -->
    <section id="about">
        <h3>About the Game</h3>
        <p>Dive deep into the world of lingualand. Explore the story, the challenges, and the excitement.</p>
    </section>

    <!-- Features Section -->
    <section id="features">
        <h3>Key Features</h3>
        <ul>
            <li>
                <strong>Feature 1:</strong> Description of an exciting feature.
            </li>
            <li>
                <strong>Feature 2:</strong> Description of another key benefit.
            </li>
            <li>
                <strong>Feature 3:</strong> Description of a third unique aspect.
            </li>
        </ul>
    </section>

    <!-- Download/CTA Section -->
    <%# <section id="download">
        <h3>Ready to Play?</h3>
        <p>Download [Game Name] now and start your adventure.</p>
        <a href="link-to-game-file" class="cta-button">Download Now</a>
    </section> %>

    <!-- Footer -->
    <footer>
        <p>&copy; 2026 Lingualogic Game Inc. All rights reserved.</p>
        <!-- Add social media links here -->
    </footer>

    <!-- Link to your JavaScript file for interactivity (if any) -->
    <script src="script.js"></script>

</body>
</html>
